<!--
Thank you for contributing to @promtior/youtrack-mcp-extended!

- What is this PR solving? Write a clear and concise description.
- Reference the issue it solves (e.g. `fixes #123`).
- Are there any parts you think require more attention from reviewers?

Before submitting, please make sure you do the following:

- Read the Contributing Guidelines: https://github.com/promptior/youtrack-mcp-extended/blob/main/CONTRIBUTING.md
- Run `npm run type-check`, `npm run lint` and `npm test` — all green.
- If you added or removed a tool, run `node scripts/build.js` so `src/server/tools-registry.ts`
  is regenerated, and commit the result.
- Add tests under `tests/` mirroring the source path.
- Update the README tool reference if the tool surface changed.
- Never commit a YouTrack token, and redact `perm:` values from any output you paste.

If this PR changes a tool's behaviour, state whether it is a breaking change for callers
(renamed parameters, changed output shape, changed `destructiveHint`).
-->
